// char vs grapheme
// count vs position
// relative vs absolute
// on top or in gap

pub struct CharIndex(pub usize);

pub struct CharGapIndex(pub usize);

// Offset? Length?
pub struct CharCount(pub usize);

pub struct GraphemeCount(pub usize);
